# Parity Expected Outputs

`generate_python_parity_fixtures.py` writes expected Python outputs here:

```text
livekit_frontend_metadata.json
*_score.json
*_embeddings.npy
```

These files can contain model scores and raw embedding vectors. Do not commit
generated expected outputs without an explicit data decision.
