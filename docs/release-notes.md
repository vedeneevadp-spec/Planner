# Release notes

Этот документ хранит пользовательские заметки к релизам Chaotika. Его нужно
обновлять для каждого production rollout и для каждой публикации store build.

Пишите заметки так, чтобы их можно было почти без правки перенести в App Store,
Google Play, RuStore или в сообщение пользователям.

## Как вести

- В `Unreleased` собираются изменения, которые еще не ушли в production.
- Перед релизом перенесите пункты из `Unreleased` в новый раздел
  `vX.Y.Z - YYYY-MM-DD`.
- Укажите каналы релиза: `Web`, `PWA`, `iOS`, `Android`.
- Описывайте пользовательский эффект, а не внутреннюю реализацию.
- Breaking changes, ручные действия пользователя и ограничения выносите отдельно.
- Если изменение не заметно пользователю, но важно для поддержки, поместите его
  в `Техническое`.

## Unreleased

<!--
Добавляйте пункты в подходящие разделы по мере готовности изменений.
Перед публикацией удаляйте пустые разделы.
-->

## v1.1.12 - 2026-08-05

Каналы: Web, PWA, Android

### Новое

- В блоке «Рутина» появились карточки актуальных покупок и уборки. Одну
  покупку можно сразу отметить купленной, а из сводных карточек можно
  перейти к полному списку.

### Улучшено

- Вверху экрана «Сегодня» теперь видны день недели и дата, а выбранный режим
  антиперегруза стал контрастнее.
- Карточки задач не повторяют сегодняшнюю дату и не показывают пустую сферу.
- Выбранный вид задач — плитки или список — сохраняется при переходах и перезагрузке.

### Исправлено

- Перенесённая на будущую дату забота сразу исчезает из списка доступных на
  сегодня и отображается в плане на выбранный день.

### Техническое

- Экран «Сегодня» разделён на личный и общий режимы без изменения
  привычного порядка и поведения; добавлены регрессионные проверки.

## v1.1.11 - 2026-08-04

Каналы: Web, PWA, Android

### Новое

- В настройках Android-виджета появились режим «Только просмотр» и отдельные
  переключатели для задач заботы и уборки.

### Улучшено

- Недельный календарь больше не занимает место задачами без времени и
  использует доступную ширину компактнее.
- Длинные названия в календаре переносятся целиком вместо сокращения с
  многоточием.
- Приложение быстрее открывает первый экран и разделы, а изображения покупок
  загружаются заметно экономнее.
- При медленном соединении переход между разделами теперь показывает понятное
  состояние загрузки, а сохранённые экраны надёжнее открываются без сети.

### Исправлено

- Сохранённые данные о подходах в разделе заботы обновляются сразу и не
  заменяются устаревшей версией при повторном открытии формы.

## v1.1.10 - 2026-08-04

Каналы: Web, PWA, Android

### Новое

- Аккаунт и связанные с ним данные теперь можно удалить самостоятельно в
  профиле после явного подтверждения.

### Улучшено

- При нестабильном интернете Chaotika надёжнее сохраняет вход в аккаунт и
  восстанавливает синхронизацию.
- Кнопка «Повторить синхронизацию» теперь сначала восстанавливает подключение к
  аккаунту, а затем обновляет данные.
- Владелец приложения может сортировать список пользователей и безопасно
  удалять аккаунты, кроме защищённого owner-аккаунта.

### Исправлено

- Прерванное обновление сессии больше не приводит к неожиданному выходу из
  аккаунта при следующем запуске.

## v1.1.9 - 2026-08-03

Каналы: Android

### Исправлено

- Android-приложение больше не закрывается сразу после запуска у уже
  авторизованных пользователей.

### Техническое

- Release-сборка Android теперь останавливается до создания APK, если в неё не
  включена корректная Firebase-конфигурация для push-уведомлений.

## v1.1.8 - 2026-08-03

Каналы: Web, PWA, Android

### Новое

- В разделе «Ещё» проверенную резервную копию личного пространства теперь
  можно восстановить после явного подтверждения. Активные записи сохраняются,
  а отсутствующие или ранее удалённые данные возвращаются из архива.

### Улучшено

- После восстановления приложение очищает устаревшие локальные данные и
  загружает актуальное состояние пространства.
- Усилена надёжность сервиса: серверные резервные копии создаются регулярно, а
  целостность архивов автоматически проверяется.

### Техническое

- Обновлены серверные компоненты и проверки совместимости базы данных.
- Усилена защита соединения с production-базой данных.

### Известные ограничения

- Восстановление доступно только для личного пространства и архива того же
  пользователя.

## v1.1.7 - 2026-07-10

Каналы: Web, PWA, Android

### Улучшено

- Резервная копия личного пространства теперь формируется из согласованного
  снимка данных и тщательнее проверяет структуру архива, связи между записями и
  вложенные изображения.
- Офлайн-изменения задач, привычек и покупок теперь жестко привязаны к текущему
  аккаунту и не переносятся при переключении пользователя.
- При ошибке отдельного изменения задачи, сферы, шаблона или покупки приложение
  сохраняет другие уже выполненные действия вместо отката всего списка.

### Исправлено

- Голосовой ввод в web-версии снова может запрашивать доступ к микрофону в
  production.
- Усилена защита сессий: уже замененный ключ сессии нельзя повторно использовать
  с другого устройства.
- Исправлены операции профиля и общих пространств при строгой изоляции данных в
  production.
- Обновление production теперь переключается на подготовленную версию целиком и
  автоматически возвращает предыдущую версию, если проверка после запуска не
  проходит.

### Известные ограничения

- Восстановление данных из пользовательского бэкапа пока не включено. Текущая
  версия позволяет скачать архив и проверить его содержимое.

## v1.0.11 - 2026-07-09

Каналы: Web, PWA, Android

### Улучшено

- Резервные копии теперь показывают понятный прогресс при подготовке, скачивании,
  сохранении и проверке файла.
- На Android файл резервной копии сохраняется в системные «Загрузки», чтобы его
  было проще найти и проверить позже.
- На мобильных устройствах выбор файла для проверки стал надежнее: приложение не
  скрывает JSON-файлы резервных копий в системном файловом менеджере.
- Резервная копия стала заметно компактнее: глобальная библиотека иконок,
  загруженная администратором для всего приложения, больше не попадает в личный
  пользовательский архив.

### Известные ограничения

- Восстановление данных из пользовательского бэкапа пока не включено. Текущая
  версия позволяет скачать архив и проверить его содержимое.

## v1.0.10 - 2026-07-09

Каналы: Web, PWA

### Улучшено

- Резервная копия личного пространства стала компактнее: удаленные задачи
  больше не попадают в архив, а выполненные задачи сохраняются только за
  последние две недели.
- Данные, связанные с задачами, уборкой, привычками, заботой о себе и иконками,
  теперь попадают в архив только вместе с актуальными родительскими записями.

### Исправлено

- Проверка файла резервной копии стала строже: приложение предупреждает о
  битых ссылках внутри архива и отсутствующих файлах вложений или аватаров.

### Известные ограничения

- Восстановление данных из пользовательского бэкапа пока не включено. Текущая
  версия позволяет скачать архив и проверить его содержимое.

## v1.0.9 - 2026-07-07

Каналы: Web, PWA

### Новое

- В разделе «Ещё» появилась возможность скачать резервную копию личного
  пространства.
- Загруженный файл резервной копии можно проверить перед восстановлением:
  приложение покажет состав архива и предупреждения, если файл относится к
  другому пользователю или пространству.

### Техническое

- Подготовлена первая безопасная часть пользовательских бэкапов: экспорт,
  проверка архива и документация по следующему этапу восстановления.
- Локальная PostgreSQL-база для разработки переведена на отдельный volume для
  PostgreSQL 18, чтобы не конфликтовать со старым локальным кластером
  PostgreSQL 16.

### Известные ограничения

- Восстановление данных из пользовательского бэкапа пока не включено. Текущая
  версия позволяет скачать архив и проверить его содержимое.

## v1.0.8 - 2026-07-06

Каналы: Web, PWA

### Улучшено

- В истории заботы о себе теперь можно открыть выполненную заботу и поправить
  стоимость, длительность, заметку и сохраненные показатели после завершения.

### Исправлено

- Удаленные заботы больше не продолжают влиять на аналитику после удаления.
- Стоимость выполненной процедуры или визита теперь корректно сохраняется в
  истории, учитывается в статистике и подставляется в форму редактирования, если
  цену нужно скорректировать позже.

### Техническое

- Локальный запуск с PostgreSQL снова работает с существующей локальной базой.

## v1.0.7 - 2026-07-02

Каналы: Web, PWA

### Техническое

- Подготовлены внутренние улучшения экрана заботы о себе, чтобы его было проще
  безопасно развивать без изменения привычного поведения.

## v1.0.6 - 2026-06-23

Каналы: Web, PWA

### Улучшено

- Иконки в окнах выбора стали крупнее, чтобы их было проще отличать и выбирать.

## v1.0.5 - 2026-06-02

Каналы: Web, PWA

### Улучшено

- Голосовой ввод теперь сразу показывает, что запись идет: в карточке появился
  компактный статус `Listening` с таймером и состояние `Processing` после
  остановки записи.
- После распознавания команды можно нажать «Ещё сказать», чтобы дописать новую
  голосовую фразу к предыдущей и повторно обработать итоговую команду.

### Техническое

- Подготовлены улучшения Android voice runtime: быстрый ненавязчивый стартовый
  сигнал без блокировки записи, vibration fallback для silent/vibrate mode,
  pre-buffer перед основной записью и новые безопасные voice metrics. Эти
  изменения попадут к установленным Android-приложениям после отдельной
  Android-сборки/публикации.

## v1.0.4 - 2026-05-13

Каналы: Web, PWA, iOS, Android

История собрана ретроспективно по коммитам `04b1956` - `51d47cb`, включая
приложение, API, PWA и native shell. Merge-коммиты и dependency-only bump'ы не
вынесены отдельными пользовательскими пунктами.

### Новое

- Запущена базовая версия Chaotika: экран «Сегодня», таймлайн, сферы жизни,
  создание и редактирование задач, планирование по датам и времени, дедлайны,
  важность, иконки и заметки.
- Добавлен дневной фокус: задачи на сегодня, просроченные задачи, задачи на
  завтра, выполненное за день и режимы нагрузки для более реалистичного плана.
- Добавлены сферы жизни с недельным балансом, статистикой активности,
  подсветкой забытых сфер и отдельными страницами сфер.
- Добавлен список покупок с активными и завершенными позициями, быстрым
  добавлением, удалением и синхронизацией.
- Добавлен планировщик уборки: зоны, шаблоны зон, расписание по дням недели,
  задачи уборки с глубиной, энергией, приоритетом, исполнителем, сезонностью,
  пропуском, переносом и завершением.
- Добавлены рутины и привычки: регулярные задачи, статистика выполнения,
  streak'и, показатели за месяц и быстрый доступ из мобильной навигации.
- Добавлены общие пространства: создание, переключение, переименование и
  удаление shared workspace, управление участниками, роли и ограниченный набор
  экранов для совместной работы.
- Добавлены профиль пользователя, аватар, управление данными аккаунта и
  отдельная страница профиля.
- Добавлена админка для пользователей, ролей, активности, количества задач,
  настроек и загрузки пользовательских иконок.
- Добавлена библиотека иконок и загрузка icon assets для задач, сфер и
  пользовательских сценариев.
- Добавлена PWA-поддержка: manifest, service worker, иконки и installable web
  experience.
- Добавлены нативные iOS и Android shell через Capacitor с мобильной
  навигацией, splash/icon assets и синхронизацией web bundle.
- Добавлены Android widgets для задач на сегодня и таймлайна: стилизованные
  карточки, действия, настройка внешнего вида, прозрачность фона и запуск
  создания задачи из виджета.
- Добавлены push-уведомления и backend-напоминания по задачам.
- Добавлена интеграция с Алисой: account linking, создание задач голосом,
  добавление покупок, чтение задач на сегодня/завтра, гибридный парсер команд и
  LLM fallback.
- Добавлена авторизация Chaotika: email/password, восстановление пароля,
  refresh-сессии, mobile session storage и OAuth-flow для Алисы.

### Улучшено

- Переработана мобильная навигация: основные разделы вынесены ближе к
  повседневным сценариям, остальные экраны доступны через «Еще».
- Привычки переведены в модель регулярных задач, чтобы рутины работали вместе с
  обычным планированием, статистикой и экраном «Сегодня».
- Улучшены карточки задач, форма создания задачи и поля рутины: добавлены более
  точные состояния, метаданные и редактирование без лишних переходов.
- Улучшены экраны сфер: баланс недели, статусы здоровья сфер и подсказки по
  областям без активности.
- Улучшена работа shared workspace: роли, доступы, участники и назначение задач
  стали согласованнее между API и web.
- Улучшен профиль и session bootstrap на web и mobile, включая восстановление
  сессии после перезапуска приложения.
- Улучшены Android widgets: визуальный стиль, кликабельные действия,
  настройки, обновление данных и отображение задач.
- Улучшены production deploy и mobile release workflow: появились проверки
  production API, синхронизация native bundle, version/build bump и сборка
  Android/iOS артефактов.

### Исправлено

- Исправлена гонка обновления mobile auth session, из-за которой приложение могло
  терять авторизацию при refresh.
- Исправлена production RLS role mismatch проблема, из-за которой
  authenticated API-запросы могли падать после deploy.
- Исправлены сценарии входа, восстановления сессии и повторной авторизации в
  web/mobile runtime.
- Исправлены ошибки shared workspace, ролей и отображения доступных экранов для
  shared runtime.
- Исправлены сценарии chaos inbox и списка покупок, включая обработку archived
  позиций и синхронизацию.
- Исправлены расчеты статистики сфер и отображение задач в карточках после
  переработки routine/habit логики.
- Исправлены mobile styles, safe-area и несколько проблем навигации после
  добавления PWA/native shell.
- Исправлены deploy-скрипты и проверки, чтобы релиз раньше ловил проблемы с
  production environment, миграциями, RLS и smoke-сценариями.

### Техническое

- Собран monorepo foundation: React/Vite web, Fastify API, shared contracts,
  TypeScript strict mode и workspace-пакеты.
- Добавлены PostgreSQL schema, миграции, seed, RLS-контекст, backup/migrate
  scripts и security checks.
- Добавлены OpenAPI generation/checks и shared API contracts для web/API
  границы.
- Добавлены offline store и sync-слои для planner, habits/routines и shopping
  list.
- Добавлены outbox, task reminders worker, push notification delivery и
  production smoke checks.
- Выделены shared API client на web и repository/route layers на backend, чтобы
  уменьшить дублирование и упростить поддержку.
- Добавлены CI, lint, format, typecheck, unit/integration/e2e проверки,
  coverage tooling и audit checks.
- Документированы architecture, shared workspace, release workflow, production
  deploy, store resubmission сценарии и incidents.

### Известные ограничения

- Production web/PWA deploy не обновляет уже установленные iOS/Android store
  builds: для native app нужен отдельный store release.
- История `v1.0.4` собрана задним числом по commit history, потому что в
  репозитории пока нет git tags для предыдущих релизных границ.

## Шаблон релиза

```markdown
## vX.Y.Z - YYYY-MM-DD

Каналы: Web, PWA, iOS, Android

### Новое

- ...

### Улучшено

- ...

### Исправлено

- ...

### Техническое

- ...

### Известные ограничения

- ...
```
