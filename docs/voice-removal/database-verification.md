# Contracts, session, database and backups — removal report

Baseline: branch `codex/today-quiet-refresh-1.1.19`, HEAD `2b40273eca2238e5ae7b89b0b514f00825b45676`; initial git status empty. Read root AGENTS.md, root/API/contracts package.json and relevant folder structure before changes. Baseline session memory contracts + backup service/helper: 22/22 passed (`/tmp/planner-voice-db-baseline.log`).

## Dependency map recorded before edits

| Element                                                                               | Consumers                                                                         | Action                                                                                                                       | Risk                                                          | Verification                                                                                 |
| ------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| contracts voice-action/command/metrics/test-corpus and planner-intent exports         | web voice feature, API voice routes, scripts/Android parity; Alice has own parser | Delete source and public/package exports                                                                                     | stranded shared consumers                                     | repository import search, contracts/API/web typecheck                                        |
| userPreferences.voiceAssistantEnabled + workspaceSettings.wakeWordTrainingModeEnabled | session contracts, repositories, clients, admin settings                          | Remove domain and persistence; wire-only forced false adapter                                                                | legacy default(true), old client patches                      | session response/patch tests including boolean validation and ordinary preferences           |
| app.users.voice_assistant_enabled; app.workspaces.wake_word_training_mode_enabled     | queries, schema types, backup export/restore, legacy preference function overload | New migration removes exact voice overload and columns without CASCADE; preserve two-argument preference function and grants | old server queries fail against new schema                    | fresh chain and previous-version upgrade in temporary isolated DB; data/settings/runtime RLS |
| app.update_current_user_preferences(text,text)                                        | historical runtime database API                                                   | Preserve/restate explicit authenticated grant and PUBLIC revoke                                                              | removing voice parameter accidentally removes normal settings | runtime-role invocation updates calendar/energy and isolates current user                    |
| backup v1 two removed columns                                                         | existing archives, strict row schema, safe anchor restore                         | Explicit optional booleans accepted and stripped at parse; new exports omit, restore whitelist omits                         | old backups rejected or removed SQL columns written           | legacy true variants, voice source, new roundtrip, malformed legacy/unknown columns          |
| chaos inbox source='voice'                                                            | Alice ChaosInboxService, historic user data, export/sync/offline, seed            | Preserve                                                                                                                     | data loss or Alice regression                                 | runtime creation/read/edit + archive restore                                                 |
| app_role='test', OAuth authorization code tables/functions/indexes/grants/RLS         | existing test users and Alice account binding/token exchange                      | Preserve                                                                                                                     | losing existing auth/account links                            | catalog equality around migration; runtime OAuth roundtrip and current sessions              |

## Environment

Docker is available via approved sandbox escalation (`docker info`: 24.0.2). Local Homebrew PostgreSQL 14 executable is unusable because ICU 71 library is missing; no host installation or configuration changes planned. All DB writes will use a uniquely named temporary Docker PostgreSQL container and explicit local test database URLs.

## Implemented and verified

- Removed all built-in `planner-intent`, `voice-action`, `voice-command`, `voice-metrics`, `voice-test-corpus` sources and their package/barrel exports. `source='voice'` remains in `chaos-inbox.ts`, DB types and seed because Alice and historic purchases still use it. No seed changes were required.
- Removed the two settings from current domain contracts, validation/refine, memory/Postgres session models/queries/inserts/updates and database schema types.
- `session.legacy-preferences.ts` is a wire-only adapter: GET session and PATCH preference/workspace responses always return false for the retired booleans; valid legacy input booleans are discarded. Voice-only user preference PATCH returns current settings without calling a repository mutation. Ordinary calendar, energy, timezone, confetti and notification settings still update. Malformed retired booleans and empty/unknown-only preference requests still fail validation. Remove this adapter only once the minimum supported web/APK clients no longer default an absent voiceAssistantEnabled to true; backup legacy compatibility must remain.
- Backup v1 stays v1. Strict row validation explicitly accepts only the two known optional boolean columns for their original tables, strips them before restore, rejects malformed values/unknown columns, and preserves all other original validation/limits. Restore anchor whitelists and new user exports omit the retired columns. New workspace exports pass through the strict normalizer, so they also omit old columns even during a code-before-schema transition.
- New migration `20260918_000106_remove_builtin_voice_settings.sql` removes the exact 3-argument preference overload, drops only the two columns without CASCADE, preserves the 2-argument preference function with authenticated EXECUTE and explicitly revokes PUBLIC EXECUTE. Historical migrations were not edited. User data, app_role test, auth/session tables and OAuth structures are preserved.

## Additional pre-existing OAuth defect found by the required DB validation

Both existing SQL Alice exchange overloads failed on actual code exchange with SQLSTATE 42702: `column reference "id" is ambiguous` in `update oauth_authorization_codes ... where id = authorization_code.code_id`. Reproduced separately on schema 000105 before000106 and again after000106 in a dedicated disposable DB. Both function definitions were byte-identical across000106:

- 9 arguments: SHA256 `16b7d343901281c3c50e4187aa49ebc24f3d1e868d2d1005f39a28fb44d3eeca`.
- 10 arguments: SHA256 `7add4c710822596c5a0599fe7950bb94dcfd4242971cf873348d5d57f0a7de1a`.

Exact four reproductions: `/tmp/planner-voice-db-oauth-baseline.log`. This was not caused by voice removal. The required Alice fresh/upgrade acceptance could not pass without the minimal separate `20260918_000107_qualify_alice_oauth_code_claim.sql`: qualify the consumed-code UPDATE with `code_row` in each overload. CREATE OR REPLACE preserves signatures/grants/security-definer; tests compare full definitions, ACL, RLS, policies and indexes, permitting only the expected alias substitution. Existing historical files remain unchanged.

## Final checks

- Focused session/legacy HTTP adapter/backups/helper/memory contracts: **39/39 PASS**, `/tmp/planner-voice-db-unit.log`.
- Full `test:api:postgres`: **96/96 PASS, zero skipped**, `/tmp/planner-voice-db-postgres.log`, using only `postgres://planner:planner@127.0.0.1:55439/voice_fresh` plus isolated fresh/upgrade DBs.
- Fresh/upgrade migration specs: **2/2 PASS**, `/tmp/planner-voice-db-migration-tests.log`. Opt-in env `VOICE_REMOVAL_MIGRATION_TEST_DATABASE_URL` is mandatory; guarded host must be localhost, database must begin `voice_`. Tests create and drop only newly generated `voice_migration_*` DBs and apply the complete historical chain. Upgrade seeds data with both obsolete booleans true before000106. Fresh seeds after removal. Both verify calendar/energy/timezones, confetti, tasks, voice purchases, `test` role, existing refresh sessions; authenticated-role CRUD and other-workspace update denial; exact removal of the obsolete overload; surviving 2-argument function/grants; pg_depend shows only own defaults/NOT NULL dependencies; function bodies contain no removed-column references.
- Real Alice OAuth under runtime authenticated role: both 9- and 10-argument exchanges succeed, consumed-code replay returns no rows, issued token refresh succeeds, revocation deactivates that session and preserves the prior independent session. All exercised in fresh and upgrade cases.
- Actual legacy and new backup export/import/restoration into the post-migration DB pass: task and Alice purchase restored; source voice preserved; profile/calendar/energy/default timezone/last-seen timezone/time mode and workspace confetti/timezone restored; re-export parses and roundtrips without removed fields. Separate malformed legacy values (`"true"`, 1, null, object, array) and unknown fields rejected.
- Fresh full production migration runner chain succeeded through000106, then runner applied000107 to the same isolated test DB. `db:migrations:check` passes.
- Isolated non-owner login role `planner_voice_test_runtime` has no table ownership. `DB_SECURITY_REQUIRE_NON_OWNER=1 node scripts/db-security-check.mjs` **PASS** after both migrations (`/tmp/planner-voice-db-security.log`).
- Scoped ESLint, contracts/API typecheck, changed-file Prettier and git diff --check are run as final checks; root handles global lint/web/UI/native checks.

## Выпуск и откат (план, не выполнялся)

1. После отдельного разрешения на выпуск сделать штатную backup/snapshot текущей БД и подтвердить возможность isolated restore. Зафиксировать release SHA, migration ledger и состояние runtime/restore roles. Не трогать секреты, OAuth links или реальные пользовательские записи.
2. Старый API/restore-helper обращается к удаляемым столбцам, поэтому нельзя оставить старые процессы активными в интервале между000106 и переключением приложения. На короткое согласованное окно остановить старый API и helper (либо сначала переключить совместимый новый код на старой схеме), применить000106/000107 транзакционно владельцем миграций, запустить только новую версию. Согласовать это с guarded deploy workflow, который иначе мигрирует до переключения release.
3. Проверить runtime non-owner/RLS, session/preferences и workspace settings, обычный CRUD, оба Alice OAuth exchange/refresh и webhook, экспорт/preview/import на изолированном тестовом аккаунте. Подтвердить, что новых voice settings нет в export, wire values всегда false, retired endpoints не мутируют данные.
4. Для rollback до применения000106 достаточно вернуть прежний release. После000106 простой rollback бинарников несовместим со схемой. Предпочтителен исправляющий forward release. Если необходим старый API, в согласованном окне добавить только два отсутствующих boolean столбца с DEFAULT false, вернуть прежнюю трехаргументную preference функцию и её authenticated grants (исторический SQL000051 без повторного применения всей истории), вернуть column UPDATE grants на два поля, затем переключить приложение. Сохранить000107 OAuth fix; не отзывать сессии/токены, не переписывать source voice и не удалять/восстанавливать целиком пользовательские таблицы. Не менять ledger исторических миграций; компенсацию оформить отдельной миграцией. Полный SQL dump restore возможен лишь отдельным решением с учетом потери записей после snapshot; это не обычный rollback.
5. Legacy false wire adapter остаётся до прекращения поддержки клиентов с default(true); существующие старые APK с уже запущенным локальным сервисом требуют обновления приложения — серверный adapter не может физически удалить их установленный native код.

## Оставшиеся ограничения

Ни production migrations/deploy, ни внешние OAuth настройки, ни реальные данные/устройства не изменялись. Тестовый контейнер `planner-voice-removal-db-20260918` оставлен root для финальной E2E проверки; после неё его нужно остановить/удалить. Подтверждение реального устройства и внешнего навыка Алисы требует отдельного smoke при выпуске.

Final gate result update: scoped ESLint, Prettier, contracts typecheck, migration hygiene and diff check PASS. Concurrent Alice test diagnostics were resolved; root full typecheck and 369/369 API tests PASS.

## Дополнительная проверка релизного перехода A→B

Новый API проверен также на отдельной старой схеме000105 (`voice_compat`): PostgreSQL regression **94 PASS / 0 FAIL / 2 intentionally skipped migration specs**; реальные GET session/PATCH preferences/PATCH workspace сохраняют ordinary settings и принудительно false legacy wire values, voice-only PATCH не меняет строки; DB flags true фактически игнорируются. Новый export без legacy полей и реальный legacy restore/profile/workspace/task/voice purchase roundtrip проходят. Подробности и логи: `/tmp/planner-voice-db-compat-report.md`.

Это позволяет рекомендовать code-first A (новый код без000106/000107) → B (добавленные000106/000107). Пока guarded deploy B применяет миграции перед переключением, работающий A уже совместим с обеими схемами. Откат B→A не требует восстановления столбцов и сохраняет OAuth fix. Ранее описанная остановка старых процессов нужна лишь при прямом одноэтапном переходе с версии ДО A; откат к той старой версии после000106 по-прежнему требует компенсации схемы. A — промежуточный этап, не завершение задачи: колонки удаляются и baseline OAuth defect исправляется только на B.

Завершение окружения: после всех root E2E временный контейнер с его volume и БД удалён, локальные dev-серверы остановлены.
